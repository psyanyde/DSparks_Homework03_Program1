<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dsparks_homework03_program1">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".SearchActivity" />
        <activity android:name=".AddMajorActivity" />
        <activity android:name=".UpdateStudentActivity" />
        <activity android:name=".StudentDetailActivity" />
        <activity android:name=".AddStudentActivity" />

        <!-- Launcher activity must declare exported when it has an intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
